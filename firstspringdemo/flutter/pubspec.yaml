name: firstspringdemo
description: A new Flutter project.
environment:
  sdk: "^3.10.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0 # Use the latest version
  flutter_riverpod: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
